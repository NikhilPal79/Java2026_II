# OPTIONAL
[prevents null pointer exception by wrapping a value that may or may not exist]
[CAN SEND EMPTY[IF STREAM IS EMPTY] OR NON EMPTY[IF STREAM IS NOT EMPTY]]
1. FIND ANY
2. FIND FIRST

# interview [segregated] [use of groupingBy]