# finally [if system exist = 0]


# Throw [when you want to throw exception explicitly ][used inside a method]
# Throws [when you want to delegate the responsibility of handling ][used with method signature]


# try with resource [try block and no need to close the resource. It will be auto closed]


============================================================================= 

# COLLECTION [It is a framework which is used to store and manipulate the group of objects]
1. It works with object only not with primitive data type.

# Types of collection -> list(Arraylist,LinkedList), Set(HashSet,LinkedHashSet), queue, map

# Wrapper classes --> Integer Char Double Boolean Float


# Generics [denoted by <_>] [Introduced to have type-safety in Java collections.][completes the both situation]
1. typesafe
2. flexibility to work with any type [Object instead of String/int/float]

# Type-Safety
1. Type safety means that a program prevents type errors at compile time by ensuring that variables,



