# MERGE CONFLICT [when 2 people change the same line of code, fit gets confused what to accept ]

# Pull vs fetch  
1. Pull [fetch + merge ]
2. Fetch [ ]


# PR FLOW 
1. developer create a feature branch 
2. changes done 
3. push to GitHub 
4. raise PR 
5. request review 
6. PR approved 
7. merge

# how to remove the changes made?
1. Revert [undo a commit by creating a new commit ][history will be there]
2. Reset [Go back in time to erase the commit][will not be in the history ]

# Use revert [ when code is pushed to remote branch ]
# Use reset [ when changes are in your local ony ]

# COMMANDS
1. git revert <commit-hash>
2. git reset --hard <commit-hash> or git reset --hard head~1


# What happen once code is pushed to GitHub ? deployment 
github ----------------server----------------pragra.com

# Compile [java code to byte code ]

# How to convert Java code to Byte code ?
Student.java----- javac Student.java ---- Student.class (bytecode)


# java is platform independent bcz of JVM.


# JAR [JAVA Archive ] 

# MAVEN [it is used to convert Java code to jar.][Build Automation Tool]


# CI/CD [CONTINUOUS INTEGRATION / CONTINUOUS DEPLOYMENT ]
- Jenkins
- GitHub Actions 























