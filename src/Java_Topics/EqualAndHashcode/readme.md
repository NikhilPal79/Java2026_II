# CONTRACT 

1. If we are overriding the equal() method then we must override the hashCode method properly [means if two objects area equal then hashCode method must return same value]
2. If two objects are unequal,according to the equal() method, it is not necessary the hashCode value can be same