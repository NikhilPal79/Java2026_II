# STATIC BLOCK [loads into the memory before even the static methods ]
no need to call static block, gets called automatically 


# INSTANCE BLOCK [gets called when the object of the class is created ]

# CONSTRUCTOR BLOCK 

# OCCURRENCE OF BLOCK
1. STATIC 
2. INSTANCE 
3. CONSTRUCTOR 

# THIS KEYWORD [used to refer the current object ]

# STRING [CHAR ARRAY ][SEQ OF CAHRS TEREATED AS A SINGLE OBJECT][NON- PRIMITIVE ]

# DATA TYPES 
1. PRIMITIVE 
2. NON- PRIMITIVE 

# WAYS TO CREATE A STRING 
1. new keywords [in HEAP ]
2. string lateral [memory saved, optimized performance ][saved to spring constant pool]

# Stack will store the ref and heap will store the object when created with NEW KEYWORDS 
# SPRING CONSTANT POOL is a special area in heap when string literals are stored.



# MUTABLE STRINGS
1. STRING BUFFER [thread-safe ] [multi code @ same time, ][inconsistency, slower due to synchronization ]
2. STRING BUILDER [not-thread safe][only one code@ one time ][ consistency, faster due to non synchronization]


# equals. & ==
== ====> compares the reference[ where it is saved in memory ]
equals. ===> compares the content []



