# INSTANCE VARIABLE & LOCAL VARIABLE 

1. INSTANCE 
inside the class but outside the method  [int x , String name]
constructor initialize the instance variable

2. LOCAL VARIABLE
local to method or constructor [present inside the constructor ]

3. STATIC VARIABLE
belongs to class rather than the instance of the class [more it's going to repeat use static]

# PARAMETER AND ARGUMENTS 

# CONSTRUCTOR 
1. DEFAULT CONS
2. PARAMETERISED CONS


# Array [collection of homogenous elements{same data type} ]

# PROPERTIES 
1. FIXED SIZE
2. index based 

# HOW TO LOOP OVER THE ELEMENT


# FOR EACH LOOP [FOR THE ARRAY]
# [for every element of arr{print element}]
syntax for( int e : arr){
sout(e)}